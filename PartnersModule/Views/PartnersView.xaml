<UserControl x:Class="PartnersModule.Views.PartnersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AccountingUI.Core.Events;assembly=AccountingUI.Core"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Header" Value="{Binding DataContext.SelectedPartner.Naziv}"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <ContentControl VerticalAlignment="Top" HorizontalAlignment="Center"
                                            Margin="0 0 7 0" Content="{Binding}"/>
                            <Button Grid.Column="1" Content="X" BorderThickness="0" Background="Transparent"
                                    Foreground="Red">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <local:CloseTabAction/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--Custom Menu-->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10 0 0 0">
            <Button Height="50" Width="50" Command="{Binding NewPartnerCommand}">
                <StackPanel>
                    <Image Source="images\folder.png" Height="30"/>
                    <TextBlock Text="Dodaj"/>
                </StackPanel>
            </Button>
        </StackPanel>
        

        <TabControl Grid.Row="1" Margin="10" prism:RegionManager.RegionName="PartnerDetailsRegion"
                    MinHeight="200">            
        </TabControl>

        <DataGrid ItemsSource="{Binding Partners}" x:Name="_listOfPartners" Grid.Row="2"
                  IsReadOnly="True" CanUserAddRows="False">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <prism:InvokeCommandAction Command="{Binding PartnerSelectedCommand}"
                                               CommandParameter="{Binding SelectedItem, ElementName=_listOfPartners}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </DataGrid>
        
    </Grid>
</UserControl>
